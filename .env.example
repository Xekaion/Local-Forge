NEXT_PUBLIC_LOCALFORGE_API=http://127.0.0.1:8000
# Optional local shared guard. Set the same value as LOCALFORGE_API_TOKEN in
# the engine shell. NEXT_PUBLIC values are embedded in the browser bundle.
NEXT_PUBLIC_LOCALFORGE_API_TOKEN=
